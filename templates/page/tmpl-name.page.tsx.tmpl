import { Component, Prop, h } from '@stencil/core';

@Component({
  tag: '[TMPL_NAME]-page',
  styleUrl: '[TMPL_NAME].page.scss',
  shadow: true,
})
export class [TMPL_NAME:CLASS_CASE]Page {
  @Prop() header_title: string = <h1>[TMPL_NAME].page HEADER</h1>;
  @Prop() section_title: string = <h2>[TMPL_NAME].page SECTION</h2>;
  render() {
		return [
			<header class="default-header">
				<div class="default-header__content container-fluid">{this.header_title}</div>
			</header>,
			<main class="main-container">
				<section class="page container-fluid">{this.section_title}</section>
			</main>
		];
  }
}
